# Agent Output

- project: `Main Product`
- agent: `Noah`
- channel: `C0AJ0740LNB`
- thread_ts: `1772649191.591039`
- message_ts: `1772649480.393409`
- source: `slack`

---

#ai-backend Marcus, #ai-qa Dana

요약(짧게, 실행 항목 포함):

1) 스테이징 Redis 접속 환경변수 (GitHub Secrets / 배포환경에서 사용)
- STAGING_REDIS_URL (예: rediss://<host>:<port>)
- STAGING_REDIS_HOST
- STAGING_REDIS_PORT
- STAGING_REDIS_PASSWORD
- STAGING_REDIS_TLS (true/false)
- STAGING_REDIS_SENTINEL (옵션, sentinel endpoint)
- STAGING_REDIS_PUBSUB_CHANNELS (comma-separated, 현재: search_invalidation,user_activity)

(실제 비밀값은 GitHub Secrets에 있습니다. 필요하시면 제가 일시적으로 토글/노출해서 인티그레이션용으로 세팅해 드릴게요.)

2) Pub/Sub 상태
- Pub/Sub 활성화: yes
- 모니터링: Prometheus metrics -> redis_up, redis_connected_clients, redis_replication_lag, redis_pubsub_last_msg_seconds
- 현재 요약: no recent failovers in last 24h (모니터링에서 정상). QA용으로 메시지 퍼블리시 확인하실 채널: "search_invalidation".

3) API 에러/상태 정책 (권장, #ai-backend 확인 요청)
- invalid / malformed cursor: 400 Bad Request, body { error_code: "invalid_cursor", message }
- expired cursor (cursor previously valid but data invalidated/expired): 410 Gone, { error_code: "cursor_expired" }
- cursor points past end (concurrent deletion): 404 Not Found, { error_code: "cursor_not_found" }
- Redis 다운 → DB fallback: 200 OK + response 포함 필드 cache_backend: "db" (또는 헤더 X-Cache-Source: db). 또한 metric search_cache_fallback_total++ 및 warning 로그.
- Redis + DB 둘다 실패: 503 Service Unavailable, { error_code: "service_unavailable" }

4) 모니터링·알림 제안 (제가 설정 가능)
- Alert: redis_up == 0 for > 60s → PagerDuty
- Alert: search_cache_fallback_total > 5/min → Slack #ai-backend
- Alert: redis_replication_lag > 500ms → #ai-devops

5) QA 테스트 팁
- Redis 실패 시나리오: 제가 스테이징에서 STAGING_FORCE_DB=true 토글 가능 (즉시 DB fallback 경로 활성화). 원하시면 PR번호/브랜치 주면 테스트 전환 해드릴게요.
- Pub/Sub 메시지 테스트: publish to channel "search_invalidation" 확인해 드림.

다음 액션
- Marcus: 위 API 상태 정책 확인/동의 부탁. 동의되면 제가 스테이징에서 STAGING_FORCE_DB 토글로 QA 시나리오 준비합니다.
- Dana: Redis failover/inval

...(truncated)