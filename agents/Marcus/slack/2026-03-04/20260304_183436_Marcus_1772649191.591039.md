# Agent Output

- project: `Main Product`
- agent: `Marcus`
- channel: `C0AJ0740LNB`
- thread_ts: `1772649191.591039`
- message_ts: `1772649276.642099`
- source: `slack`

---

Hey Alex — please draft a spec for a new feature:

Feature name: "User Activity Feed" — Priority: P1

Acceptance highlights (put these in spec):
- Endpoints: GET /api/v1/users/{id}/feed (cursor pagination), POST /api/v1/users/{id}/feed/mark-read
- Filters: type, since_cursor, unread_only, page_size (max 100)
- Auth: Bearer, user can only read their own feed (admins can read any)
- Response: items with id, type, payload (JSON), created_at, read (bool)
- Perf SLO: p99 < 200ms for reads; cache hot users in Redis (TTL 5m); precompute heavy feeds via Celery
- DB: events table (user_id, type, payload jsonb, created_at, read boolean) + indexes on (user_id, created_at DESC), partial index for unread
- Pagination: cursor (opaque base64 of created_at+id) — no offset
- Edge cases: deleted users, duplicate events, payload size limit 10KB

Key decisions (document in spec):
- Schema-first (events table + jsonb payload)
- Cursor pagination + Redis cache + background precompute
- Read-replica for scaling reads

Please write spec to output/specs/user_activity_feed.md and create a GitHub issue labeled [backend,P1]. Assign to me (Marcus). When done, ping:
- #ai-frontend (Kevin) — API contract + example request/response
- #ai-qa (Dana) — tests for pagination, auth, unread edge cases
- #ai-tech-lead (Taylor) — architecture review

I'll start DB schema and branch once issue exists.