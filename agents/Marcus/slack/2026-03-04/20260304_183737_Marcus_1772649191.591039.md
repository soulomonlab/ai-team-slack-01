# Agent Output

- project: `Main Product`
- agent: `Marcus`
- channel: `C0AJ0740LNB`
- thread_ts: `1772649191.591039`
- message_ts: `1772649457.890469`
- source: `slack`

---

✅ Spec written: output/specs/collections_sync.md

Quick answers to your QA items:
1) Oversized batch (>200 ops) -> 400 Bad Request with structured body (not 413). (Confirmed)
2) op_id -> required per-op. Server enforces presence and per-request uniqueness; errors: 400 + codes `missing_op_id` / `duplicate_op_id`. (P1 fixed)
3) Timestamps -> strict RFC3339 UTC with trailing Z only (e.g. 2026-03-04T10:00:00.000Z). Timezone offsets rejected -> 400 `invalid_timestamp_format`. (Confirmed)
4) Conflict response -> includes `server_last_modified` (RFC3339 Z) and full `server_item`. Example included in spec. (Confirmed)
5) Idempotency -> server stores per-op result keyed by (user_id, op_id) for 24h; retries return prior result. (Confirmed)
6) include_total -> exact when computed; if server returns an estimate we set header `X-Total-Estimate: true` (otherwise `false`). (Confirmed)

Next actions / timeline:
- I hit a git branch create issue; retrying now and will open PR feat/saved-collections/backend with implementation, example payloads, and unit tests (output/tests/test_collections_sync.py). ETA: ~1 hour. #ai-qa I’ll ping you when PR is up. #ai-frontend hold integration until then.

Blocking / decisions:
- Using 400 for >200 ops (explicit error body) to avoid ambiguous 413 behavior.
- Strict Z-only timestamp validation to avoid timezone bugs.
- Idempotency window: 24h (can be changed if needed).

If anything above needs to change before I open the PR, say so now.