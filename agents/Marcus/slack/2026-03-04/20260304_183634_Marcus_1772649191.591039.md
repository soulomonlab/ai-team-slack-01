# Agent Output

- project: `Main Product`
- agent: `Marcus`
- channel: `C0AJ0740LNB`
- thread_ts: `1772649191.591039`
- message_ts: `1772649394.662169`
- source: `slack`

---

Got it — I'll implement the backend API and create branch feat/saved-collections/backend. Quick answers & decisions so you can wire the frontend:

1) Pagination
- cursor field: next_cursor (string token). also return has_more: boolean.
- total_count: not provided by default (expensive). If frontend needs it, call ?include_total=true — server will return approx_total (may be slow).

2) Item shape (API JSON)
- id: string (uuid)
- last_modified: RFC3339 UTC string (e.g. "2026-03-04T12:34:56Z") — used for LWW
- deleted_at: nullable RFC3339 UTC (null = active). We do NOT use separate deleted boolean (keeps sync simple).
- other fields: payload (object) / metadata as per spec.

3) Sync endpoint
- POST /api/v1/collections/sync
- max batch size: 200 ops per request (server-enforced). Recommend client cap = 200 (matches your plan).
- Request: array of ops {op_id (client-generated idempotency), type: create/update/delete, item:{id,...}, last_modified}
- Response: 200 with per-op results array: {op_id, status: "ok"|"conflict"|"error", server_item?: {...}, server_last_modified?: "...", reason?: "conflict_reason"}
  - On conflict, server returns merged/current server_item and status "conflict". Client can decide accept/merge.
- We always return per-op granular results (no 207).

4) Auth
- Expect Authorization: Bearer <token> only (JWT/OAuth). 401 for missing/invalid.

5) Rate-limit headers
- We'll surface standard headers:
  - X-RateLimit-Limit
  - X-RateLimit-Remaining
  - X-RateLimit-Reset (epoch seconds)
- Also return Retry-After on 429.

Key backend decisions (summary)
- Conflict resolution: server LWW by last_modified; if client last_modified <= server_last_modified => conflict returned.
- Soft-delete implemented via deleted_at only.
- Idempotency via client op_id (required).
-

...(truncated)