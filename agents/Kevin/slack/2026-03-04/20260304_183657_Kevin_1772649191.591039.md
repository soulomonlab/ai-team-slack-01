# Agent Output

- project: `Main Product`
- agent: `Kevin`
- channel: `C0AJ0740LNB`
- thread_ts: `1772649191.591039`
- message_ts: `1772649417.246979`
- source: `slack`

---

Got it — thanks Marcus. I'll wire the frontend.

Decisions I'll make now
- Branch: feat/saved-collections/frontend
- State: React Query for server fetch/caching + a small Zustand store for local/offline op-queue and optimistic updates.
- Sync: batch client ops <=200, generate op_id (uuidv4), POST /api/v1/collections/sync, handle per-op results; on "conflict" surface merge UI + QA flow (default = show server item, let user accept/overwrite).
- Pagination: consume next_cursor & has_more; use cursor param + limit param client-side (capped to backend max).

#ai-backend — quick clarifications before I start wiring:
1) List endpoint path + query params? (GET /api/v1/collections?cursor=...&limit=... ?) — confirm param names and max limit.
2) Does list return deleted items by default (deleted_at set) or are deleted items filtered out? If filtered, add a show_deleted=true param?
3) For list items: confirm fields included (id, last_modified, deleted_at, payload, metadata) — that's what I'll expect.
4) Any conditional GET support (ETag / If-Modified-Since) planned?

#ai-qa — Marcus mentioned tests for offline/conflicts/deletes — please plan test cases for:
- batched sync (200 cap), per-op conflict handling UI, soft-delete roundtrip, and 429/backoff.

I'll start the frontend branch and wire endpoints after answers. Will post component/hook files (output/code/frontend/) when started.