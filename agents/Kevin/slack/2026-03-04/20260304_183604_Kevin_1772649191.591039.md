# Agent Output

- project: `Main Product`
- agent: `Kevin`
- channel: `C0AJ0740LNB`
- thread_ts: `1772649191.591039`
- message_ts: `1772649365.085569`
- source: `slack`

---

Read the spec: output/specs/saved_collections.md — thanks Marcus. I'll start frontend work.

Plan & key decisions (will implement):
- Branch: feat/saved-collections/frontend (creating now)
- Service worker: Workbox for precache + runtime caching; background sync + retry/backoff
- Client storage: IndexedDB (idb) for local mirror + sync queue; keep last_modified timestamps for LWW
- Sync strategy: batched ops, optimistic UI, show pending state, mark soft-deletes with deleted_at for offline deletes
- Pagination: client uses keyset cursors (cursor from API); default pageSize=50, enforce cap=200 client-side
- Cache: short-lived UI cache (60s) + invalidate on local write
- Error/conflict UX: surface conflict modal with simple undo/accept; automated server-side merge where safe

Questions for #ai-backend (Marcus) — need concrete field names/limits:
1) Collections list response: cursor field name (cursor / next_cursor)? is total_count provided?
2) Item shape: timestamp field name (last_modified?), soft-delete flag name (deleted_at or deleted boolean + timestamp)?
3) Sync endpoint: max batch size per request? How are per-op results returned on conflict (per-item status + merged object)?
4) Auth: expect Authorization: Bearer <token> header only?
5) Any custom rate-limit headers to surface to user (e.g., X-RateLimit-Remaining)?

Hand-offs:
- #ai-backend — please confirm above fields & batch limits so I can wire client sync.
- #ai-qa — I'll ping when initial QA build/PR is ready for edge-case tests (offline, conflicts, deletes).

I'll start implementation and post PR URL once ready.